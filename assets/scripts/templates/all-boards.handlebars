{{#each boards}}
  <div class="container">
    <div class="{{this.name}}" data-id="{{this.id}}">
        <p>Board: {{this.name}}</p>
      <a href="#" data-toggle="modal">Get Details</a>
    </div>
  </div>
{{/each}}
